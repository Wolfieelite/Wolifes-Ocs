import { Tab, Tabs } from "@mui/material";

export default function NavigationBar() {
  return (
    <nav className="flex flex-row border">
      <Tabs>
        <Tab />
      </Tabs>
    </nav>
  );
}
